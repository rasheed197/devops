#!/usr/bin/bash

# Open Terminal --> CTRL+ALT+D

# The clear command in Linux is used to clear the terminal screen. 

# When executed, it removes all previous output and commands from 
# the visible screen, providing a clean workspace. 

clear

# Another way to clear the terminals is to use --> CTRL + L